---
lc_num: 300
link: "https://leetcode.com/problems/longest-increasing-subsequence/"
title: "Longest Increasing Subsequence"
topic: "1-D Dynamic Programming"
lc_difficulty: Medium
---

# [300. Longest Increasing Subsequence](https://leetcode.com/problems/longest-increasing-subsequence/)


Given an integer array nums, return the length of the longest strictly increasing subsequence.

A subsequence is a sequence that can be derived from an array by deleting some or no elements without changing the order of the remaining elements. For example, [3,6,2,7] is a subsequence of the array [0,3,1,6,2,2,7].

 

**Example 1:**
```
Input: nums = [10,9,2,5,3,7,101,18]
Output: 4
Explanation: The longest increasing subsequence is [2,3,7,101], therefore the length is 4.
```
**Example 2:**
```
Input: nums = [0,1,0,3,2,3]
Output: 4
```
**Example 3:**
```
Input: nums = [7,7,7,7,7,7,7]
Output: 1
``` 

Constraints:

- 1 <= nums.length <= 2500
- -10<sup>4</sup> <= nums\[i\] <= 10<sup>4</sup>
 

> Follow up: Can you come up with an algorithm that runs in O(n log(n)) time complexity?

---

## Solution

### Intuition
`dp[i]` = length of the longest increasing subsequence ending at index `i`. For each `i`, scan all `j < i`: if `nums[j] < nums[i]`, then `dp[i] = max(dp[i], 1 + dp[j])`. Each element is at least a length-1 subsequence by itself, so all dp values initialize to 1. The answer is `max(dp)`. A top-down memoized version of the same recurrence appears in the [[0.1DDynamicProgrammingGuide|guide's Sequence DP section]]; this note uses the tabulated form directly. The O(n log n) patience sorting approach is a problem-specific alternative that exploits LIS structure rather than a general Sequence DP technique: it maintains the smallest possible tail for each LIS length, using [[0.BinarySearchGuide|Binary Search]] to find the replacement position.

### Approach 1: O(n^2) tabulation

For each element, check every earlier element to find the longest extendable subsequence.

```python
class Solution:
    def lengthOfLIS(self, nums: list[int]) -> int:
        n = len(nums)
        dp = [1] * n

        for i in range(1, n):
            for j in range(i):
                if nums[j] < nums[i]:
                    dp[i] = max(dp[i], 1 + dp[j])

        return max(dp)
```

#### Tabulation walkthrough

`nums = [10, 9, 2, 5, 3, 7, 101, 18]`. Each step fills one dp cell by scanning all earlier cells.

```freeform
const lisNums=[10,9,2,5,3,7,101,18];
const lisSteps=[
 {label:"Init",i:-1,dp:[1,1,1,1,1,1,1,1],active:-1,action:"All dp values start at 1 (each element is a subsequence of length 1).",line:1},
 {label:"i=0",i:0,dp:[1,1,1,1,1,1,1,1],active:0,action:"nums[0]=10. No earlier elements. dp[0] stays 1.",line:2},
 {label:"i=1",i:1,dp:[1,1,1,1,1,1,1,1],active:1,action:"nums[1]=9. No j where nums[j]<9. dp[1] stays 1.",line:2},
 {label:"i=2",i:2,dp:[1,1,1,1,1,1,1,1],active:2,action:"nums[2]=2. No j where nums[j]<2. dp[2] stays 1.",line:2},
 {label:"i=3",i:3,dp:[1,1,1,2,1,1,1,1],active:3,action:"nums[3]=5. j=2: nums[2]=2<5, dp[3]=max(1, 1+1)=2.",line:3},
 {label:"i=4",i:4,dp:[1,1,1,2,2,1,1,1],active:4,action:"nums[4]=3. j=2: nums[2]=2<3, dp[4]=max(1, 1+1)=2.",line:3},
 {label:"i=5",i:5,dp:[1,1,1,2,2,3,1,1],active:5,action:"nums[5]=7. Best from j=3 or j=4: dp[5]=max(1, 2+1)=3.",line:3},
 {label:"i=6",i:6,dp:[1,1,1,2,2,3,4,1],active:6,action:"nums[6]=101. Extends j=5: dp[6]=max(1, 3+1)=4.",line:3},
 {label:"i=7",i:7,dp:[1,1,1,2,2,3,4,4],active:7,action:"nums[7]=18. Extends j=5: dp[7]=max(1, 3+1)=4.",line:3},
 {label:"Result",i:-1,dp:[1,1,1,2,2,3,4,4],active:-1,action:"max(dp)=4. One LIS is [2, 3, 7, 101].",line:4,done:true},
];
const lisRoot=document.createElement("section");lisRoot.setAttribute("role","region");lisRoot.setAttribute("aria-label","[300. Longest Increasing Subsequence](https://leetcode.com/problems/longest-increasing-subsequence/) interactive walkthrough 1");lisRoot.style.cssText="font:14px system-ui;width:min(100%,740px);box-sizing:border-box;border:1px solid #c8cdd2;border-radius:10px;padding:12px;background:#ffffff;color:#17202a;";
lisRoot.innerHTML=`<style>.lis-graph{display:block;width:100%;height:auto}.lis-box{padding:9px 10px;border:1px solid #d5d8dc;border-radius:8px;background:#f8f9f9;min-width:0;margin-top:8px}.lis-code{margin-top:8px;padding:8px 10px;border-radius:8px;background:#f1f3f5;font:13px/1.55 monospace;overflow-x:auto}.lis-controls{display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin-top:10px}.lis-controls button,.lis-controls select{min-height:44px;font-size:14px}.lis-controls button{padding:6px 12px}.lis-counter{margin-left:auto}@media(max-width:520px){.lis-controls button{flex:1 1 72px}.lis-speed{display:flex;align-items:center;gap:8px;flex:1 1 100%}.lis-speed select{flex:1}.lis-counter{width:100%;margin-left:0;text-align:center}.lis-code{font-size:12px}}</style>
<svg class="lis-graph" viewBox="0 0 700 180" preserveAspectRatio="xMidYMid meet" role="img" aria-label="LIS O(n^2) tabulation walkthrough"><g data-layer="cells"></g></svg>
<div style="display:flex;gap:12px;flex-wrap:wrap;font-size:12px;margin-bottom:8px"><span>Orange = current i</span><span>Blue = part of an LIS of length 4</span><span>Gray = other</span></div>
<div data-role="status" aria-live="polite" style="min-height:22px;padding:9px 10px;border-radius:8px;background:#eef2f3"></div>
<div class="lis-box"><strong>dp array</strong><div data-role="state" style="margin-top:5px;font-family:monospace"></div></div>
<div class="lis-code" data-role="code"><div data-line="1">1  dp = [1] * n</div><div data-line="2">2  for j in range(i): if nums[j] < nums[i]</div><div data-line="3">3    dp[i] = max(dp[i], 1 + dp[j])</div><div data-line="4">4  return max(dp)</div></div>
<div class="lis-controls"><button data-action="previous">Previous</button><button data-action="play">Play</button><button data-action="next">Next</button><button data-action="reset">Reset</button><label class="lis-speed">Speed <select data-action="speed"><option value="1200">Slow</option><option value="700" selected>Normal</option><option value="350">Fast</option></select></label><strong class="lis-counter" data-role="counter"></strong></div>`;
var lisNs="http://www.w3.org/2000/svg",lisLayer=lisRoot.querySelector('[data-layer="cells"]');
for(var idx=0;idx<8;idx++){var x=18+idx*86;var g=document.createElementNS(lisNs,"g");g.dataset.idx=String(idx);g.innerHTML='<rect x="'+x+'" y="38" width="66" height="60" rx="8" fill="#e5e7eb" stroke="#6b7280" stroke-width="3"></rect><text x="'+(x+33)+'" y="28" text-anchor="middle" font-size="13" fill="#4b5563">'+lisNums[idx]+'</text><text data-role="dp" x="'+(x+33)+'" y="68" text-anchor="middle" dominant-baseline="central" font-size="20" font-weight="700" fill="#17202a">1</text><text x="'+(x+33)+'" y="125" text-anchor="middle" font-size="12" fill="#4b5563">dp['+idx+']</text>';lisLayer.appendChild(g)}
var lisI=0,lisT=null;function lisStop(){if(lisT!==null)clearInterval(lisT);lisT=null;lisRoot.querySelector('[data-action="play"]').textContent="Play"}
function lisR(){var s=lisSteps[lisI];for(var g of lisLayer.querySelectorAll('g[data-idx]')){var i=Number(g.dataset.idx);var active=i===s.active;var inLIS=s.done&&(i===2||i===4||i===5||i===6);var rect=g.querySelector("rect");rect.setAttribute("fill",active?"#f8c471":inLIS?"#aed6f1":"#e5e7eb");rect.setAttribute("stroke",active?"#d35400":inLIS?"#2471a3":"#6b7280");rect.setAttribute("stroke-width",active||inLIS?"5":"3");g.querySelector('[data-role="dp"]').textContent=String(s.dp[i])}lisRoot.querySelector('[data-role="status"]').textContent=s.label+": "+s.action;lisRoot.querySelector('[data-role="state"]').textContent="dp = ["+s.dp.join(", ")+"]";for(var el of lisRoot.querySelectorAll('[data-line]')){var a=Number(el.dataset.line)===s.line;el.style.cssText=a?"background:#f8c471;color:#17202a;font-weight:700":"";if(a)el.setAttribute("aria-current","step");else el.removeAttribute("aria-current")}lisRoot.querySelector('[data-role="counter"]').textContent="Step "+(lisI+1)+" / "+lisSteps.length;lisRoot.querySelector('[data-action="previous"]').disabled=lisI===0;lisRoot.querySelector('[data-action="next"]').disabled=lisI===lisSteps.length-1}
lisRoot.querySelector('[data-action="previous"]').onclick=function(){lisStop();lisI=Math.max(0,lisI-1);lisR()};lisRoot.querySelector('[data-action="next"]').onclick=function(){lisStop();lisI=Math.min(lisSteps.length-1,lisI+1);lisR()};lisRoot.querySelector('[data-action="reset"]').onclick=function(){lisStop();lisI=0;lisR()};lisRoot.querySelector('[data-action="play"]').onclick=function(){if(lisT!==null){lisStop();return}if(lisI===lisSteps.length-1)lisI=0;lisRoot.querySelector('[data-action="play"]').textContent="Pause";lisT=setInterval(function(){if(lisI===lisSteps.length-1){lisStop();return}lisI++;lisR()},Number(lisRoot.querySelector('[data-action="speed"]').value));lisR()};lisRoot.querySelector('[data-action="speed"]').onchange=function(){if(lisT!==null){lisStop();lisRoot.querySelector('[data-action="play"]').click()}};lisR();display(lisRoot);
```

The two indices with dp=4 are indices 6 and 7. One LIS is `[2, 3, 7, 101]`.

### Approach 2: O(n log n) patience sorting, a problem-specific alternative

Maintain a `tails` array where `tails[k]` = smallest tail value of all increasing subsequences of length `k+1`. For each number, use binary search to find where it fits. If it extends the longest known subsequence, append it. Otherwise, replace the first tail that is >= the number to keep the smallest possible tail at that length.

```python
import bisect

class Solution:
    def lengthOfLIS(self, nums: list[int]) -> int:
        tails: list[int] = []

        for num in nums:
            pos = bisect.bisect_left(tails, num)
            if pos == len(tails):
                tails.append(num)
            else:
                tails[pos] = num

        return len(tails)
```

> `tails` is always sorted. `bisect_left` finds the leftmost position where `num` can replace a tail without violating strictly-increasing order. The length of `tails` at the end equals the LIS length, but `tails` itself is NOT necessarily a valid subsequence.

#### Tails-array walkthrough

`nums = [10, 9, 2, 5, 3, 7, 101, 18]`. Each step processes one number and either appends to or replaces within `tails`.

```freeform
var ptNums=[10,9,2,5,3,7,101,18];
var ptSteps=[
 {label:"Start",num:"-",numIdx:-1,tails:[],pos:"-",act:"append",action:"tails is empty.",line:1},
 {label:"num=10",num:10,numIdx:0,tails:[10],pos:0,act:"append",action:"tails empty, append 10. tails = [10].",line:2},
 {label:"num=9",num:9,numIdx:1,tails:[9],pos:0,act:"replace",action:"bisect_left finds pos 0 (9 < 10). Replace tails[0]. tails = [9].",line:3},
 {label:"num=2",num:2,numIdx:2,tails:[2],pos:0,act:"replace",action:"bisect_left finds pos 0 (2 < 9). Replace tails[0]. tails = [2].",line:3},
 {label:"num=5",num:5,numIdx:3,tails:[2,5],pos:1,act:"append",action:"bisect_left finds pos 1 (5 > 2). Append. tails = [2, 5].",line:2},
 {label:"num=3",num:3,numIdx:4,tails:[2,3],pos:1,act:"replace",action:"bisect_left finds pos 1 (3 < 5). Replace tails[1]. tails = [2, 3].",line:3},
 {label:"num=7",num:7,numIdx:5,tails:[2,3,7],pos:2,act:"append",action:"bisect_left finds pos 2 (7 > 3). Append. tails = [2, 3, 7].",line:2},
 {label:"num=101",num:101,numIdx:6,tails:[2,3,7,101],pos:3,act:"append",action:"bisect_left finds pos 3 (101 > 7). Append. tails = [2, 3, 7, 101].",line:2},
 {label:"num=18",num:18,numIdx:7,tails:[2,3,7,18],pos:3,act:"replace",action:"bisect_left finds pos 3 (18 < 101). Replace tails[3]. tails = [2, 3, 7, 18].",line:3},
 {label:"Result",num:"-",numIdx:-1,tails:[2,3,7,18],pos:"-",act:"-",action:"len(tails) = 4. The LIS length is 4.",line:4,done:true},
];
var ptRoot=document.createElement("section");ptRoot.setAttribute("role","region");ptRoot.setAttribute("aria-label","LIS patience sorting walkthrough");ptRoot.style.cssText="font:14px system-ui;width:min(100%,740px);box-sizing:border-box;border:1px solid #c8cdd2;border-radius:10px;padding:12px;background:#ffffff;color:#17202a;";
ptRoot.innerHTML='<style>.pt-nums{display:flex;gap:6px;justify-content:center;flex-wrap:wrap;margin:10px 0}.pt-num{width:52px;padding:7px 4px;border:3px solid #6b7280;border-radius:8px;background:#e5e7eb;text-align:center;font-size:15px;font-weight:700}.pt-state{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:8px}.pt-box{padding:9px 10px;border:1px solid #d5d8dc;border-radius:8px;background:#f8f9f9;min-width:0;overflow-wrap:break-word}.pt-code{margin-top:8px;padding:8px 10px;border-radius:8px;background:#f1f3f5;font:13px/1.55 monospace;overflow-x:auto}.pt-controls{display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin-top:10px}.pt-controls button,.pt-controls select{min-height:44px;font-size:14px}.pt-controls button{padding:6px 12px}.pt-counter{margin-left:auto}@media(max-width:520px){.pt-state{grid-template-columns:1fr}.pt-controls button{flex:1 1 72px}.pt-speed{display:flex;align-items:center;gap:8px;flex:1 1 100%}.pt-speed select{flex:1}.pt-counter{width:100%;margin-left:0;text-align:center}.pt-code{font-size:12px}}</style><div data-role="status" aria-live="polite" style="min-height:22px;padding:9px 10px;border-radius:8px;background:#eef2f3"></div><div class="pt-nums" data-role="numsrow" aria-label="nums = [10,9,2,5,3,7,101,18]"></div><div style="display:flex;gap:12px;flex-wrap:wrap;font-size:12px;margin-bottom:8px"><span style="color:#d35400">&#9632; Orange = current number</span><span style="color:#2471a3">&#9632; Blue = already processed</span><span style="color:#6b7280">&#9632; Gray = not yet processed</span></div><div class="pt-state"><div class="pt-box"><strong>Current number</strong><div data-role="num" style="margin-top:5px;font-family:monospace"></div></div><div class="pt-box"><strong>tails array</strong><div data-role="tails" style="margin-top:5px;font-family:monospace"></div></div></div><div class="pt-code" data-role="code"><div data-line="1">1  tails = []</div><div data-line="2">2  pos == len(tails): append num</div><div data-line="3">3  else: tails[pos] = num (replace)</div><div data-line="4">4  return len(tails)</div></div><div class="pt-controls"><button data-action="previous">Previous</button><button data-action="play">Play</button><button data-action="next">Next</button><button data-action="reset">Reset</button><label class="pt-speed">Speed <select data-action="speed"><option value="1200">Slow</option><option value="700" selected>Normal</option><option value="350">Fast</option></select></label><strong class="pt-counter" data-role="counter"></strong></div>';
var ptNumsRow=ptRoot.querySelector('[data-role="numsrow"]');for(var pi=0;pi<ptNums.length;pi++){var nd=document.createElement("div");nd.className="pt-num";nd.dataset.index=String(pi);nd.textContent=String(ptNums[pi]);ptNumsRow.appendChild(nd)}
var ptI=0,ptT=null;function ptStop(){if(ptT!==null)clearInterval(ptT);ptT=null;ptRoot.querySelector('[data-action="play"]').textContent="Play"}
function ptR(){var s=ptSteps[ptI];ptRoot.querySelector('[data-role="status"]').textContent=s.label+": "+s.action;for(var el of ptNumsRow.querySelectorAll(".pt-num")){var idx=Number(el.dataset.index);var isActive=idx===s.numIdx;var isProcessed=idx<s.numIdx;el.style.background=isActive?"#f8c471":isProcessed?"#aed6f1":"#e5e7eb";el.style.borderColor=isActive?"#d35400":isProcessed?"#2471a3":"#6b7280";el.style.borderWidth=isActive?"5px":"3px"}
ptRoot.querySelector('[data-role="num"]').textContent="num="+s.num+(s.pos!=="-"?" pos="+s.pos:"");ptRoot.querySelector('[data-role="tails"]').textContent="["+s.tails.join(", ")+"]"+(s.tails.length?" (length "+s.tails.length+")":"(empty)");for(var el2 of ptRoot.querySelectorAll('[data-line]')){var a=Number(el2.dataset.line)===s.line;el2.style.cssText=a?"background:#f8c471;color:#17202a;font-weight:700":"";if(a)el2.setAttribute("aria-current","step");else el2.removeAttribute("aria-current")}ptRoot.querySelector('[data-role="counter"]').textContent="Step "+(ptI+1)+" / "+ptSteps.length;ptRoot.querySelector('[data-action="previous"]').disabled=ptI===0;ptRoot.querySelector('[data-action="next"]').disabled=ptI===ptSteps.length-1}
ptRoot.querySelector('[data-action="previous"]').onclick=function(){ptStop();ptI=Math.max(0,ptI-1);ptR()};ptRoot.querySelector('[data-action="next"]').onclick=function(){ptStop();ptI=Math.min(ptSteps.length-1,ptI+1);ptR()};ptRoot.querySelector('[data-action="reset"]').onclick=function(){ptStop();ptI=0;ptR()};ptRoot.querySelector('[data-action="play"]').onclick=function(){if(ptT!==null){ptStop();return}if(ptI===ptSteps.length-1)ptI=0;ptRoot.querySelector('[data-action="play"]').textContent="Pause";ptT=setInterval(function(){if(ptI===ptSteps.length-1){ptStop();return}ptI++;ptR()},Number(ptRoot.querySelector('[data-action="speed"]').value));ptR()};ptRoot.querySelector('[data-action="speed"]').onchange=function(){if(ptT!==null){ptStop();ptRoot.querySelector('[data-action="play"]').click()}};ptR();display(ptRoot);
```

The final tails `[2, 3, 7, 18]` has length 4, matching the LIS length. Note that `[2, 3, 7, 18]` is not the actual LIS (it is `[2, 3, 7, 101]`). The array stores smallest tails, not the subsequence itself.

### Complexity
- **Approach 1:** O(n^2) time, O(n) space.
- **Approach 2:** O(n log n) time, O(n) space.

### Key concepts to review
- [[Dynamic Programming]] - dp\[i\] builds on all prior smaller-element positions.
- [[0.BinarySearchGuide|Binary Search]] - patience sort uses bisect to locate the replacement position in O(log n).
- [[0.GreedyGuide|Greedy]] - patience sort greedily keeps the smallest possible tail at each LIS length.

### Common pitfalls
- Returning `dp[n-1]` instead of `max(dp)`: the longest LIS need not end at the last element.
- Using `nums[j] <= nums[i]` allows equal elements, which violates the strictly increasing requirement.
- In the patience-sort approach, thinking `tails` is itself a valid LIS. It stores smallest tails, not actual subsequence elements.
- Forgetting to initialize all dp values to 1. Starting at 0 would undercount subsequences of length 1.
